G.
Fetcha json filerna =>
Gör dom åtkomliga =>
Gör en scroll meny som visar länder =>
Då ska den informationen visas i egen vy i main =>
Och städerna ska visas =>
Gör varje stad klickbar =>
Som visar antalet invånare =>
I stadvyn, knapp för att spara invånarantal till local storage =>
I huvudmenyn så ska alla besökta städer visas =>
Där ska dom besökta städernas invånare adderas =>
Finnas en knapp som raderar local storage =>


VG.
I stadvyn, visa vädret med API =>
I stadvyn, hämta mer info om staden via API och visa.


// en funktion som fetchar bägge json filer,
 sorterar dem och lägger bara länder i menyn, denna funktion kollar även localstorage för besökta städer, 
 lägger även till detta i menyn

// funktion som när man klickar på ett land, tömmer main och sedan printar alla aktuella städer i main

// en funktion som printar info om staden man klickat på, ny div inne i main under städerna som visar aktuell stad med knapp för besökt

// en funktion som sparar besökta städer i localstorage som objekt där varje stad lagras tillsammans med sina invånare

// en funktion som tömmer localstorage på besökta städer 




header -> väljer ett land ->väljer en stad -> Ny div skapas med besökare/info/väderrapport.